[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback：所有路由都導回 index.html（React Router 必要）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
